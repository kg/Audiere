#include <stdio.h>
#include "default_io.hpp"


////////////////////////////////////////////////////////////////////////////////

void* ADR_CALL DefaultOpen(void* opaque, const char* filename)
{
  return fopen(filename, "rb");
}

////////////////////////////////////////////////////////////////////////////////

void ADR_CALL DefaultClose(void* file)
{
  fclose((FILE*)file);
}

////////////////////////////////////////////////////////////////////////////////

int ADR_CALL DefaultRead(void* file, void* buffer, int size)
{
  return fread(buffer, 1, size, (FILE*)file);
}

////////////////////////////////////////////////////////////////////////////////

int ADR_CALL DefaultSeek(void* file, int destination)
{
  return (0 == fseek((FILE*)file, destination, SEEK_SET));
}

////////////////////////////////////////////////////////////////////////////////

int ADR_CALL DefaultTell(void* file)
{
  return ftell((FILE*)file);
}

////////////////////////////////////////////////////////////////////////////////
